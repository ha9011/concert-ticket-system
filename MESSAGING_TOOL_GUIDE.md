# 작업 종류별 도구 선택 가이드 — Redis · DB · Kafka · RabbitMQ

> "결제니까 Kafka, 메일이니까 Redis" 처럼 **도메인 단위로 도구를 외우면 평생 헷갈린다.**
> 도구는 **도메인이 아니라 "작업 종류"에 붙는다.** 한 도메인(결제) 안에서도 작업마다 다른 도구가 분업한다.

마지막 갱신: 2026-06-15

---

## 0. 이 문서를 한 문장으로

> **"이 도메인에 뭘 쓰지?"가 아니라 "이 *작업*이 무슨 종류지?"를 먼저 물어라.**
> 작업 종류가 정해지면 도구는 거의 자동으로 따라온다.

---

## 1. 가장 중요한 멘탈 모델 — Queue냐 Log냐

메시지 도구가 헷갈리는 이유는 "다 메시지 큐"라고 한 덩어리로 보기 때문이다. 사실 **두 부류**다.

```
┌─ Queue 모델 (할 일 목록) ──────  Redis List, RabbitMQ
│   "누가 가져가면 사라짐"
│   목적: 일을 누군가 한 명이 처리 (작업 분배)
│
└─ Log 모델 (사건 기록부) ───────  Kafka, Redis Stream
    "읽어도 안 사라짐, 여러 명이 각자 읽음"
    목적: 일어난 일을 여러 곳에 전파 + 다시 읽기(replay) 가능
```

| | Queue | Log |
|---|---|---|
| 비유 | **식당 주문표** — 요리사가 떼가면 사라짐 | **신문** — 구독자 100명이 각자 읽음, 안 사라짐 |
| 소비 | 한 메시지 = 한 소비자 | 한 메시지 = 여러 소비자 각자 |
| 재처리 | 기본적으로 불가 | 과거 메시지 replay 가능 |
| 질문 | "이 일을 **누군가 한 명**이 처리해줘" | "이런 일이 **일어났다**고 여러 시스템에 알려줘" |

> 💡 **헷갈리면 도구 이름부터 떠올리지 말고 "이건 Queue 일이야, Log 일이야?"를 먼저 물어라.** 답이 절반으로 줄어든다.

---

## 2. 도구 4개의 자리

| 도구 | 부류 | 한 줄 정체 | 규모 감각 |
|---|---|---|---|
| **Redis List** | Queue | 제일 가벼운 큐. 이미 Redis 있으면 덤 | 초당 수천~수만, 가벼운 작업 |
| **Redis Stream** | Log(미니) | "Redis 안의 미니 Kafka". ack·재처리 내장 | 중간 규모 |
| **RabbitMQ** | Queue | 전통 브로커. **복잡한 라우팅**·작업 분배 강함 | 초당 수만, 라우팅 복잡 |
| **Kafka** | Log | 대용량 이벤트 스트림. **재생·다중 소비** | 초당 수십만~수백만 |

---

## 3. 의사결정 트리 — "언제 뭘 쓰나"

```
1. 유실돼도 괜찮은 가벼운 작업인가? (메일·알림·썸네일)
   └ YES → Redis List   (이미 Redis 있으면 끝. 더 고민 X)

2. 유실 안 되는데, 규모는 중간이고 Redis 환경인가?
   └ YES → Redis Stream  (ack/재처리가 공짜로 따라옴)

3. "어떤 메시지는 A로, 어떤 건 B,C로" 복잡한 라우팅/작업분배가 핵심인가?
   └ YES → RabbitMQ

4. 대용량 + 여러 시스템이 같은 이벤트를 각자 소비 + 나중에 재생해야 하나?
   └ YES → Kafka
```

위에서부터 내려가며 "내 상황이 여기 멈추나?"를 본다. **대부분은 1번 아니면 4번에서 멈춘다.**

---

## 4. ⭐ 작업 종류별 도구 + Redis의 역할 (이 문서의 핵심)

같은 도메인 안에서도 **작업마다 도구가 다르다.** 각 작업의 학습 자료를 링크로 연결한다.

| 작업 종류 | 무슨 일 | 도구 | Redis의 역할 | 학습 자료 |
|---|---|---|---|---|
| **캐싱** | 자주 읽는 데이터 빠르게 | Redis | 읽기 가속 (Cache-Aside) | [String 실무 §Cache-Aside](redis-quests/01-string/02-실무.md) |
| **중복 방지** | 결제 연타·재시도 한 번만 | Redis | `SETNX` 멱등성 키 | [부록-멱등성](redis-quests/01-string/부록-멱등성.md) |
| **동시성 제어** | 같은 자원 동시 접근 차단 | Redis | 분산 락 | [부록-분산락](redis-quests/01-string/부록-분산락.md) |
| **요청 제한** | 초당 N회 제한 | Redis | 레이트 리밋 | [부록-rate-limit](redis-quests/01-string/부록-rate-limit.md) |
| **원자적 복합 연산** | 확인 후 처리 한 방에 | Redis | Lua 스크립트 | [부록-lua-스크립트](redis-quests/01-string/부록-lua-스크립트.md) |
| **비동기 작업 큐** | 메일·알림 백그라운드 처리 | Redis List | FIFO 작업 큐 | [List 실무 §메시지 큐](redis-quests/02-list/02-실무.md) |
| **신뢰성 큐** | 유실 안 되는 작업 큐 | Redis Stream / List | Reliable Queue (BLMOVE+processing) | [List 실무 §Reliable Queue](redis-quests/02-list/02-실무.md) |
| **정합성(진실)** | 돈·잔액·거래기록 | **🔴 RDB** | ❌ Redis 단독 불가 | [List 면접질문 §신뢰성](redis-quests/02-list/03-면접질문.md) |
| **이벤트 전파** | 여러 시스템에 사건 알림 | **Kafka** | (Log 모델) | [List 실무 §브로커 비교](redis-quests/02-list/02-실무.md) |

> 핵심: **Redis = 효율·방어 담당 / DB = 진실 담당 / Kafka = 전파 담당.** 경쟁이 아니라 분업이다.

---

## 5. 실전 ① — 결제 한 건의 분업

"결제에 Redis 쓸 수 있냐?"는 잘못된 질문이다. **결제의 *어느 부분*이냐**가 맞다. 결제 한 건에 Redis·DB·Kafka가 다 등장한다.

| 결제 플로우 단계 | 작업 종류 | 도구 |
|---|---|---|
| 결제 버튼 연타 막기 | 중복 방지(멱등성) | **Redis** ([부록-멱등성](redis-quests/01-string/부록-멱등성.md)) |
| 같은 상품 동시 주문 제어 | 분산 락 | **Redis** ([부록-분산락](redis-quests/01-string/부록-분산락.md)) |
| 1분에 결제 N회 제한 | 레이트 리밋 | **Redis** ([부록-rate-limit](redis-quests/01-string/부록-rate-limit.md)) |
| **실제 돈 차감·잔액·거래기록** | **정합성(진실)** | **🔴 RDB (절대 Redis 단독 ❌)** |
| 결제 완료 → 정산·배송·알림에 알림 | 이벤트 전파 | **Kafka** |

```
[결제 처리 본체] ──→ DB에 거래 확정 (여기까지가 진짜 결제)
       │
       └──→ "결제됐다!" 이벤트 발행 → Kafka → 정산팀·배송팀·알림팀이 각자 받아감
                                              ↑ 이 전파 부분만 Kafka
```

**비유:**
- **Redis = 문지기** — 빨리 거르고 막음 (틀려도 복구 가능)
- **DB = 금고** — 진짜 돈 기록 (절대 틀리면 안 됨)
- **Kafka = 방송실** — 끝났다고 여러 부서에 전파

---

## 6. 실전 ② — 이 프로젝트 환영 메일이 List로 "충분한" 이유

[ROADMAP](REDIS_STUDY_ROADMAP.md)의 `mail:queue`는 Redis List다. 왜 충분한지 의사결정 트리로 설명할 수 있어야 한다.

| 질문 | 환영 메일 | 판정 |
|---|---|---|
| 유실되면 치명적? | ❌ 메일 한 통 안 가도 가입은 성공, 재발송 가능 | List OK |
| 트래픽 폭발? | ❌ 가입자 수준 (초당 몇 건) | List OK |
| 여러 시스템이 같은 이벤트 소비? | ❌ 그냥 보내면 끝 | Log 불필요 |
| 복잡한 라우팅? | ❌ 없음 | RabbitMQ 불필요 |
| 이미 Redis 있나? | ✅ 세션·인증에 쓰는 중 | List가 덤으로 딱 |

→ **"가벼운 작업 + 유실 약간 OK + 이미 Redis 있음"** = 트리 1번에서 멈춤. **여기에 Kafka 붙이면 오버엔지니어링.**

**반대로 "결제 완료 이벤트"였다면?** 유실 불가 + 여러 시스템이 같은 이벤트 소비 + 재처리 → **Kafka 후보.** (같은 "비동기 처리"라도 메일이냐 결제냐에 따라 답이 갈린다.)

---

## 7. 절대 원칙 — 진실은 DB, Redis는 효율·방어

> **돈·재고·예약 같은 "진실"은 DB가 책임진다. Redis는 그 앞단에서 중복·동시성·과다요청을 막는 문지기일 뿐이다.**

왜 진실을 Redis에 두면 안 되나:
- Redis는 **휘발성** + failover 시 **마지막 쓰기 유실** 가능 (비동기 복제의 구조적 한계).
- 그래서 Redis 락/멱등성은 **효율(빠르게 막기)** 용도이고, **정합성의 최종 보루는 DB**다.
- 자세히: [부록-분산락](redis-quests/01-string/부록-분산락.md)의 Redlock 논쟁, [List 면접질문 G(실전)](redis-quests/02-list/03-면접질문.md).

---

## 8. 학습 전략 — "카프카만 공부해도 되나?"

**결론: 카프카만은 안 된다.** 실무에서 가장 자주 만나는 건 오히려 Redis 큐(가볍고 어디나 있음). 둘은 대체재가 아니라 다른 도구라, 하나만 알면 "왜 골랐는지"를 설명 못 한다 — 그게 면접·설계에서 진짜 보는 능력이다.

| 도구 | 깊이 | 이유 |
|---|---|---|
| **Redis List/Stream** | 🔵🔵🔵 깊게 | 어디나 있음, 기본기 (현재 학습 중) |
| **Kafka** | 🔵🔵🔵 깊게 | 백엔드 면접·MSA 단골. 투자 가치 최고 |
| **RabbitMQ** | 🔵 개념만 | "라우팅 강한 전통 큐" 포지션. 필요할 때 깊이 |

**추천 순서:** Redis 큐(지금) → Queue/Log 멘탈 모델 체득 → Kafka 깊게(Log 대표) → RabbitMQ·Stream은 "차이점만".

핵심은 **모든 도구를 깊게가 아니라, "Queue vs Log" 모델 + 대표 2개(Redis·Kafka) 제대로 + 나머지는 포지셔닝.**

---

## 9. 면접/설계에서 쓸 한 줄 무기

> **"가벼운 비동기 작업은 Redis 큐, 대용량·다중소비·재생이 필요한 이벤트는 Kafka. 핵심 질문은 '한 명이 처리하고 끝낼 일(Queue)이냐, 여러 시스템에 퍼뜨리고 기록으로 남길 일(Log)이냐'다. 그리고 돈 같은 진실은 항상 DB, Redis는 그 앞단 효율·방어용이다."**

이 한 문장으로 거의 모든 "왜 이거 골랐어요?"에 답할 수 있다.

---

## 관련 학습 자료

- 전체 로드맵: [REDIS_STUDY_ROADMAP.md](REDIS_STUDY_ROADMAP.md)
- **캐싱 / 멱등성 / 분산락 / 레이트리밋 / Lua** (String 부록):
  - [부록-멱등성](redis-quests/01-string/부록-멱등성.md) · [부록-분산락](redis-quests/01-string/부록-분산락.md) · [부록-rate-limit](redis-quests/01-string/부록-rate-limit.md) · [부록-lua-스크립트](redis-quests/01-string/부록-lua-스크립트.md)
  - [String 실무 (Cache-Aside)](redis-quests/01-string/02-실무.md)
- **메시지 큐 / Reliable Queue / DLQ / 브로커 비교** (List):
  - [List 실무](redis-quests/02-list/02-실무.md) · [List 시나리오](redis-quests/02-list/03-시나리오.md) · [List 면접질문](redis-quests/02-list/03-면접질문.md)
